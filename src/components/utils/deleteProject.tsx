import { Project } from '@/types/types';
import { getProjectsFromLocalStorage } from './localStorageDatabase';

export const deleteProject = async (project: Project) => {
  //   const existingProjects = await getProjectsFromLocalStorage();
};
